<%= flash[:notice] %>

<div class=title>
<strong>Title:</strong>
<p><%= @book.title %></p>
</div>

<div class=body>
<strong>Body:</strong>
<p><%= @book.body %></p>
</div>

<%= link_to "edit", edit_book_path(@book.id) %>
<%= link_to 'Back', books_path, class: "back" %>
